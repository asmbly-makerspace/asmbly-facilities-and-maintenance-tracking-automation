# New Problem Report Request Received

This lambda function is triggered when a new problem report is submitted via a Google Form. It parses the form response, creates a ClickUp task, a Discourse post, and sends a Slack notification.
